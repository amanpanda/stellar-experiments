let _,createSinks,createSource,sendPayment;_771‍.w("lodash",[["default",["_"],function(v){_=v}]]);_771‍.w("./src/utils",[["createSinks",["createSinks"],function(v){createSinks=v}],["createSource",["createSource"],function(v){createSource=v}],["sendPayment",["sendPayment"],function(v){sendPayment=v}]]);






const doExperiment = async (nSinks) => {
  // Create sinks
  const sinks = await createSinks(nSinks);
  const source = await createSource();

  const res = await Promise.all(_.map(sinks, sink => sendPayment({
    source,
    destination: sink,
  })));

  _771‍.g.console.log("res.length", res.length);
};

doExperiment(3);





 